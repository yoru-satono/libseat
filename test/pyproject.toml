[project]
name = "libseat-server-test"
version = "0.1.0"
description = "libseat-server 端到端 API 集成测试"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "openpyxl>=3.1.5",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
addopts = "-v --tb=short"
testpaths = ["tests"]
